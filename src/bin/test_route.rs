fn main() {
    println!("RTF_LLINFO = {:#x}", libc::RTF_LLINFO);
    println!("RTF_UP = {:#x}", libc::RTF_UP);
    println!("RTF_GATEWAY = {:#x}", libc::RTF_GATEWAY);
    println!("RTF_HOST = {:#x}", libc::RTF_HOST);
}
